[project]
name = "py-gameoflife"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipython>=9.0.2",
    "numpy>=2.2.4",
]

[dependency-groups]
dev = [
    "ipykernel>=6.29.5",
]
